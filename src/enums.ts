export enum ButtonType {
  Primary = 'primary',
  Secondary = 'secondary',
}

export const enum CounterButtonType {
  Add = 'add',
  Substract = 'substract',
}

export const enum ArrowDirection {
  Prev = 'prev',
  Next = 'next',
}
